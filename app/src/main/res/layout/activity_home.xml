<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background"
    android:padding="28dp">

    <!-- Top spacer + app name -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Weander"
            android:textSize="52sp"
            android:fontFamily="serif"
            android:textColor="@color/text_primary"
            android:layout_marginBottom="6dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="a micro-adventure generator"
            android:textSize="14sp"
            android:textColor="@color/text_secondary"
            android:layout_marginBottom="52dp" />

        <!-- Round Weander button -->
        <Button
            android:id="@+id/btn_weander"
            android:layout_width="210dp"
            android:layout_height="210dp"
            android:layout_gravity="center_horizontal"
            android:text="Weander"
            android:textSize="26sp"
            android:fontFamily="serif"
            android:textColor="@color/on_primary"
            android:background="@drawable/bg_weander_btn"
            android:stateListAnimator="@null"
            android:elevation="4dp" />

    </LinearLayout>

    <!-- Stats + Journal link at bottom -->
    <TextView
        android:id="@+id/text_stats"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:textColor="@color/text_secondary"
        android:textSize="13sp"
        android:layout_marginBottom="12dp" />

    <Button
        android:id="@+id/btn_journal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Journal"
        android:textColor="@color/primary"
        android:background="@drawable/bg_btn_secondary"
        android:stateListAnimator="@null"
        android:padding="12dp" />

</LinearLayout>
